

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>CategoryMgr</title>
    <link href="~/CSS/bootstrap/bootstrap.min.css" type="text/css"/>
    <script src="~/scripts/jquery-1.8.3.min.js"></script>
    <script type="text/javascript">
        function edit(obj) {
            /*
            1、构建第一个td的子元素，并填充到第一个td
            2、构建第二个td的子元素，并填充到第二个td
            3、添加更新按钮，添加取消按钮; 按钮追加到第二个td中；

            */
            var tr = obj.parentNode.parentNode;
            var tdName = obj.parentNode.children[0];
            var tdDesc = obj.parentNode.children[1];
            var td = obj.parentNode;
            var span = document.createElement('span');
            span.innerHTML = "<input type='text' onkeydown='enterSave(event," + id + ")' maxlength=30 />"
     + " <a href='#' onclick='javascript:saveEdit(" + id + ");return false;'>保存</a>"
     + " <a href='#' onclick='javascript:cancelEdit();return false;'>取消</a>";
            $(tdName).innerHtml("<input type='text' onkeydown='enterSave(" + id+","+name+","+desc + ")' value="+" maxlength=30 />");
        }
        
        function saveEdit(id,name,desc) {
            
        }

        function cancelEdit(name,desc) {

        }
    </script>
</head>
<body>
    <div>
        <h4>category</h4>
        <hr />
        <table class="table-striped">
            @if (ViewBag.Categorys != null)
            {
                <thead>
                    <th>
                        <td>标题</td>
                        <td>描述</td>
                        <td>操作</td>
                    </th>
                </thead>
                <tbody>
                    @foreach (var c in ViewBag.Categorys)
                    {
                    <tr>
                        <td>@c.c_name</td>
                        <td>@c.c_desc</td>
                        <td class="edit" id="@c.id"><a onclick="edit(this);">编辑</a>|<a>删除</a></td><!--td的类属性用于索引到操作-->
                    </tr>
                    }
                </tbody>
            }
        </table>
        <div class="nav-divider">
            <form class="form-group" action="CreateCategory" method="post">
                <legend>标签名称</legend>
                <input type="text" placeholder="标题…" name="CategoryName"/>
                <input type="text" placeholder="描述…" name="CategoryDesc" />
                <button type="submit" class="btn">注册</button>
            </form>
        </div>
    </div>
    <p>
    </p>
</body>
</html>
